import type { Metadata } from "next";
import { siteConfig } from "@/lib/config";
export const metadata: Metadata = { title: "About", description: `About ${siteConfig.business.displayName}.` };
export default function AboutPage() { const { business, professional } = siteConfig; return <main className="interior-page"><section className="section-wrap interior-hero"><p className="eyebrow">The practice <span /></p><h1>Room for the<br />details that <em>matter.</em></h1><p>{business.description}</p></section><section className="section-wrap profile-section"><div className="about-stamp">A considered<br />way of working.</div><div><p className="eyebrow">Professional profile <span /></p><h2>{professional.name}</h2><p className="about-copy">This profile is a placeholder for owner-approved professional information. Replace it with verified qualifications, experience, and the scope of services offered by {business.displayName}.</p><div className="profile-line"><span>{professional.designation}</span><span>{professional.qualifications}</span></div></div></section></main>; }
